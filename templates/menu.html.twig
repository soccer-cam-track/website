<nav class="navbar navbar-expand-lg bg-light mb-4">
	<div class="container">
		<a class="navbar-brand" href="#">
			<img src="{{ asset("gfx/sct-logo.png") }}" width="200"/>
		</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarNav">
			<ul class="navbar-nav">
				<li class="nav-item">
					<a class="nav-link {% if route == "/" %}active{% endif %}" href="{{ path("/") }}">
						🏠 Home
					</a>
				</li>
				<li class="nav-item">
					<a class="nav-link {% if route == "/people" %}active{% endif %}" href="{{ path("/people") }}">
						👪 People
					</a>
				</li>
				<li class="nav-item dropdown">
					<a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
						📅 Events
					</a>
					<ul class="dropdown-menu">
						<li>
                            {% for event in events %}
							<a class="dropdown-item" href="{{ path('/event?e='~event.tag) }}">
								{{ event.name }}
							</a>
                            {% endfor %}
						</li>
					</ul>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="https://discord.gg/j7a9RbaZAt">
						💬 Discord
					</a>
				</li>

			</ul>
		</div>
	</div>
</nav>
